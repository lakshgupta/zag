# Build artifacts
zig-out/
zig-cache/
.zig-cache/

# Distribution packaging
dist/*/

# Editor / IDE
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# bin
*.out

# Vendored zig binaries (downloaded at build/test time -- embedded into
# the zag binary via `-Dzig_payload=<path>`, used as a smoke-runner
# fixture path, or referenced as a runtime zig binary on the dev
# machine). Staged locally per-developer; never committed to GitHub.
vendor/
